# AGENTS.md

## 项目目标

本仓库实现 Omarchy 的 CodexBar 插件。插件以 `codexbar serve` 作为后端服务入口，在 QML 中展示 CodexBar 状态并提供用户操作。

## 架构边界

- `codexbar serve` 是服务启动、状态和操作能力的唯一事实来源；优先使用它公开的接口，不在插件中复制其业务逻辑。
- QML 层负责服务生命周期协调、连接状态映射、界面展示和用户交互。
- 将服务通信与视觉组件分离，使后端协议变化不会扩散到整个界面。
- 从实际 CLI 帮助、配置或既有代码读取端口、套接字和启动参数，不硬编码未经确认的值。
- 除非任务明确要求，不修改用户的 `~/.config/omarchy`、Hyprland 或其他系统配置。

## 工作流程

1. 修改前先检查仓库结构、现有文档和 `codexbar serve --help`；以当前代码和命令输出为准。
2. 沿完整链路理解改动：服务启动 → 就绪检测 → QML 连接 → 状态展示 → 用户操作 → 结果或错误反馈。
3. 保持改动聚焦；协议、服务管理和 UI 变更应尽量位于各自边界内。
4. 使用仓库已有的格式化、静态检查和测试命令。仓库尚未定义命令时，不虚构命令；在交付说明中记录实际完成的验证。
5. 涉及服务连接时，至少验证服务未启动、启动中、已连接、连接失败和重连后的行为。

## QML 约定

- 明确表示 `stopped`、`starting`、`ready`、`error` 等状态，避免用多个布尔值隐式拼出状态机。
- 网络、进程和文件操作保持异步，不阻塞 QML 渲染路径。
- 后端响应先在适配层转换为稳定的 QML 模型，再交给展示组件。
- 用户操作应有进行中、成功或失败反馈；错误信息应可理解且可恢复。
- 组件销毁或服务退出时，清理定时器、连接、订阅和子进程，避免重复启动服务。
- 沿用 Omarchy 当前主题、间距和交互模式；新增样式优先复用现有 token 或组件。
- 对缺失字段、服务版本差异和短暂断线采取防御性处理，界面保持可用。

## 服务与进程管理

- 同一用户会话只应有一个插件负责的 `codexbar serve` 实例。
- 启动后等待明确的就绪信号再发送请求；不要把“进程已创建”当作“服务可用”。
- 区分插件启动的服务与用户已经运行的服务。插件只清理自己拥有的进程。
- 日志需包含足够的上下文用于定位启动、连接和协议错误，但不得输出令牌、凭据或完整敏感载荷。

## 完成标准

- 服务和 QML 的职责边界保持清晰。
- 正常路径与断线、启动失败等关键失败路径均得到处理。
- QML 主线程没有新增阻塞操作，也没有明显的生命周期泄漏。
- 已运行所有可用且相关的检查；无法自动验证的行为已在交付说明中列出。
- 新的配置项、启动参数或用户可见行为已同步到项目文档。
